🛒 Supermarket – Python Django Web-Projekt

Eine einfache, einsteigerfreundliche Webanwendung für einen Supermarkt-Katalog / -Verwaltung

📌 Projektübersicht

Dieses Projekt ist eine Python-Webanwendung, umgesetzt mit dem Django-Framework.
Ziel ist es, eine einfache Supermarkt-Webseite / Backend-API zu erstellen, die Produkte, Kategorien und ggf. Verkäufe oder Nutzer verwaltet.

Die Anwendung dient als Lernprojekt, um zu verstehen, wie man mit Python, Django, Datenmodellen und Web-Routen eine vollständige Webanwendung baut.

📦 Abhängigkeiten installieren

Dieses Projekt benötigt einige externe Python-Pakete.
Alle benötigten Abhängigkeiten sind in der Datei requirements.txt aufgelistet.

1. Virtuelle Umgebung erstellen (empfohlen)
python -m venv venv

Aktivieren:

Windows

venv\Scripts\activate


macOS / Linux

source venv/bin/activate

2. Requirements installieren
pip install -r requirements.txt


Damit werden automatisch alle benötigten Bibliotheken installiert (z. B. Django).

3. Installation prüfen
pip list

❗ Wichtiger Hinweis

Falls beim Installieren Fehler auftreten:

prüfen, ob Python installiert ist

prüfen, ob pip aktuell ist:

python -m pip install --upgrade pip
